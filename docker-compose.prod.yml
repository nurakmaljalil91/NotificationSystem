services:
  notificationsystem:
    image: ${WEBAPI_IMAGE:-notificationsystem:latest}
    env_file:
      - ./.env
    environment:
      ASPNETCORE_URLS: http://0.0.0.0:8080
    ports:
      - "8080:8080"
    restart: unless-stopped
